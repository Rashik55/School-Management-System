rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read and write for all collections in the school management database
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

